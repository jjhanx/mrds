---
description: 코드 수정 후 항상 Git에 커밋·푸시하기
alwaysApply: true
---

# Git 커밋·푸시

수정 후엔 늘 알아서 Git에 올린다. 코드를 **고치거나 기능을 추가/변경할 때마다** 변경 사항을 커밋·푸시한다.

1. 수정이 완료되면 `git add`로 변경된 파일을 스테이징한다.
2. 의미 있는 커밋 메시지로 `git commit` 한다 (예: `fix(auth): ...`, `feat(ui): ...`).
3. `git push`로 원격 저장소에 푸시한다.

- Git 사용자 정보(`user.name`, `user.email`)가 설정되지 않아 커밋이 실패하면, 사용자에게 설정 방법을 안내한다.
- 푸시 전에 가능하면 `git status`로 스테이징된 변경만 커밋되는지 확인한다.
